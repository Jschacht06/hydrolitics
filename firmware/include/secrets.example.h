#pragma once

// Copy this file to "secrets.h" (same folder) and fill in your real values.
// secrets.h is gitignored so credentials are never committed.

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASS "your-wifi-password"

#define MQTT_HOST "192.168.1.10" // broker IP or hostname
#define MQTT_PORT 1883
#define MQTT_USER ""             // leave "" if broker needs no auth
#define MQTT_PASS ""
#define MQTT_CLIENT_ID "hydrolitics-esp32"



